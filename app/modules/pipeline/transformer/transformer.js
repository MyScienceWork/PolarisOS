// @flow

const _ = require('lodash');
const Utils = require('../../utils/utils');
const Logger = require('../../../logger');

async function transform(object: Object, transformers: Array<any>): Promise<Object> {
    return object;
}

module.exports = transform;
