const chai = require('chai');
/* const AppWeb = require('../build/modules/utils/web');
const Organization = require('../build/modules/entities/organization/organization');
const OrganizationModel = require('../build/modules/entities/organization/models/organizations');
// const utils = require('../build/modules/utils/utils');

const should = chai.should();


describe('App.AppWeb#forge_ok_response', () => {
    it('should return correct change property', () => {
        const obj = new Organization(OrganizationModel);
        let res = AppWeb.forge_ok_response(obj, 'post');
        res.should.have.property('message');
        res.should.have.property('change', 'POST_organization');

        res = AppWeb.forge_ok_response(obj, 'put');
        res.should.have.property('message');
        res.should.have.property('change', 'PUT_organization');

        res = AppWeb.forge_ok_response(obj, 'delete');
        res.should.have.property('message');
        res.should.have.property('change', 'DELETE_organization');
    });

    it('should return correct message property', () => {
        const obj = new Organization(OrganizationModel);
        let res = AppWeb.forge_ok_response(obj, 'post');
        res.should.have.property('message', 'Organization is successfully added');

        res = AppWeb.forge_ok_response(obj, 'put');
        res.should.have.property('message', 'Organization is successfully modified');

        res = AppWeb.forge_ok_response(obj, 'delete');
        res.should.have.property('message', 'Organization is successfully removed');
    });

    it('should return the entity for POST & PUT', () => {
        const obj = new Organization(OrganizationModel);
        let res = AppWeb.forge_ok_response(obj, 'post');
        res.should.have.property('entity');

        res = AppWeb.forge_ok_response(obj, 'put');
        res.should.have.property('entity');
    });
    });*/
